{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    <div class="container d-flex justify-content-center mt-5 mb-5 align-items-center">
        <div class="w-50 ">
            <a href="{{ path('app_register', { 'type': 'user' }) }}" type="submit" class="btn btn-primary">Register As
                Normal
                User</a>
            <a href="{{ path('app_register', { 'type': 'company' }) }}" type="submit" class="btn btn-primary">Register
                as Company Member</a>

        </div>
    </div>
{% endblock %}
